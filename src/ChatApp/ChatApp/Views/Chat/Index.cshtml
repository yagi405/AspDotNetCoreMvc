@model ChatIndexViewModel

@{
    ViewData["Title"] = "Index";
}

<form asp-action="Index">
    <div class="form-inline">
        <textarea asp-for="Message" class="input-message"></textarea>
        <input class="btn" type="submit" value="送信" />
    </div>
</form>

<div class="chat-logs">
    @foreach (var chatLog in Model.Details)
    {
        <div>
            <div class="chat-log">
                <span class="post-at">@chatLog.PostAt</span>
                <div class="message">
                    <p>@chatLog.Message</p>
                </div>
            </div>
        </div>
    }
</div>